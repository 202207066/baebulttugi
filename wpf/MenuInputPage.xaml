<Page x:Class="wpf.MenuInputPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1100"
      Title="MenuInputPage" Background="#F5F6FA"
      FontFamily="Segoe UI, Malgun Gothic">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="🍳 메뉴(레시피) 등록 및 관리" FontSize="24" FontWeight="Bold" Foreground="#1A202C"/>
            <TextBlock Text="새로운 메뉴를 등록하고 레시피별 식재료 및 알러지 유발 성분을 관리합니다." FontSize="13" Foreground="#718096" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="20" BorderBrush="#E2E8F0" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="새 메뉴 입력" FontSize="16" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,20"/>

                    <TextBlock Text="메뉴명" FontSize="13" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,6"/>
                    <TextBox x:Name="TxtMenuName" Height="38" Padding="10,0" VerticalContentAlignment="Center" BorderBrush="#CBD5E0" Margin="0,0,0,15"/>

                    <TextBlock Text="열량 (kcal)" FontSize="13" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,6"/>
                    <TextBox x:Name="TxtCalories" Height="38" Padding="10,0" VerticalContentAlignment="Center" BorderBrush="#CBD5E0" Margin="0,0,0,15"/>

                    <TextBlock Text="주요 구성 식재료 (쉼표로 구분)" FontSize="13" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,6"/>
                    <TextBox x:Name="TxtIngredients" Height="38" Padding="10,0" VerticalContentAlignment="Center" BorderBrush="#CBD5E0" Margin="0,0,0,15" ToolTip="예: 닭고기, 땅콩, 대두"/>

                    <TextBlock Text="포함된 알러지 유발 물질" FontSize="13" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,6"/>
                    <TextBox x:Name="TxtAllergies" Height="38" Padding="10,0" VerticalContentAlignment="Center" BorderBrush="#CBD5E0" Margin="0,0,0,25" ToolTip="예: 땅콩, 밀"/>

                    <Button x:Name="BtnSaveMenu" Content="✨ 레시피 저장하기" Height="42" Background="#3182CE" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnSaveMenu_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="20" BorderBrush="#E2E8F0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="등록된 레시피 데이터베이스" FontSize="16" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,15"/>

                    <DataGrid x:Name="DgMenuList" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                              Background="Transparent" BorderThickness="0" RowHeaderWidth="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E2E8F0">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F7FAFC"/>
                                <Setter Property="Foreground" Value="#4A5568"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10,12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="메뉴명" Binding="{Binding MenuName}" Width="1.5*"/>
                            <DataGridTextColumn Header="열량" Binding="{Binding Calories}" Width="0.8*"/>
                            <DataGridTextColumn Header="포함 식재료" Binding="{Binding Ingredients}" Width="2*"/>
                            <DataGridTextColumn Header="유발 알러지" Binding="{Binding Allergies}" Width="1.5*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>