<Window x:Class="wpf.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="로그인" Height="220" Width="360" WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">아이디:</Label>
        <TextBox x:Name="UsernameTextBox" Grid.Row="0" Grid.Column="1" Margin="8,4" />

        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">비밀번호:</Label>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="8,4" />

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" x:Name="ErrorText" Foreground="Red" Margin="4" />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="80" Margin="4" Click="OkButton_Click">확인</Button>
            <Button Width="80" Margin="4" Click="CancelButton_Click">취소</Button>
        </StackPanel>
    </Grid>
</Window>