<Window x:Class="wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:wpf"
        mc:Ignorable="d"
        Title="어린이급식 알레르기 관리 시스템" Height="700" Width="1300"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" materialDesign:ElevationAssist.Elevation="Dp4">
            <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                       Text="어린이급식센터 알레르기 프리 식단 관리 시스템"/>
        </materialDesign:ColorZone>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Padding="20" Margin="0,0,20,0" materialDesign:ElevationAssist.Elevation="Dp2">
                <StackPanel>
                    <TextBlock Text="메뉴 정보 관리" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,20"/>

                    <TextBox x:Name="txtMenuName" Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                             materialDesign:HintAssist.Hint="메뉴명" Margin="0,0,0,15"/>

                    <TextBox x:Name="txtIngredients" Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                             materialDesign:HintAssist.Hint="식재료" Margin="0,0,0,15"/>

                    <TextBox x:Name="txtAllergy" Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                             materialDesign:HintAssist.Hint="알레르기 코드" Margin="0,0,0,30"/>

                    <Button Content="구글 시트에 추가" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,0,10" Click="Button_Click"/>
                    <Button Content="새로고침" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Padding="10">
                <DataGrid x:Name="dgMenuData" AutoGenerateColumns="True" 
                          CanUserAddRows="False" SelectionMode="Single">
                </DataGrid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>